{
  "name": "stelstone-com",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "stelstone.com — the Stelstone landing, built with Stelstone on one Cloudflare Worker.",
  "scripts": {
    "dev": "node --env-file-if-exists=.env node_modules/.bin/astro dev",
    "build": "astro build && node scripts/postbuild.mjs",
    "preview": "npm run build && wrangler dev -c worker/wrangler.jsonc",
    "deploy": "npm run build && wrangler deploy -c worker/wrangler.jsonc"
  },
  "dependencies": {
    "@stelstone/admin-ui": "^0.21.1",
    "@stelstone/astro-blocks": "^0.8.0",
    "@stelstone/blocks": "^0.5.0",
    "@stelstone/server": "^0.26.0",
    "@stelstone/worker": "^0.7.0",
    "astro": "^7.0.0",
    "stelstone": "^6.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
